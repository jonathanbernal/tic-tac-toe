const Gameboard = function () {
    let board = [];

    const initializeBoard = () => {
        // fill with Cell objects
    };

    const getBoard = () => board;

    const placePlayerMark = (player, cellPosition) => {

    };

    const printBoard = () => {
        
    }

    return {getBoard, placePlayerMark, printBoard};
}

export {Gameboard};