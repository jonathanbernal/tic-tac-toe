const Player = (name, mark) => {
    
    const getMark = () => mark;
    const getName = () => name;

    const play = () => {
        let playerChoice = "";
        // logic
        return playerChoice;
    }

    return {getMark, getName, play};
}

export {Player};